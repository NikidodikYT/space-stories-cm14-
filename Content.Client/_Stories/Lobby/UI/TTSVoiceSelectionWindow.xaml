<controls:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    x:Class="Content.Client._Stories.Lobby.UI.TTSVoiceSelectionWindow"
    Title="{Loc 'stories-tts-voice-selection-title'}"
    SetSize="800 500"
    MinSize="800 500"
>
    <BoxContainer Orientation="Vertical" VerticalExpand="True" Margin="5" SeparationOverride="5">

        <BoxContainer Orientation="Horizontal">
            <LineEdit Name="SearchBar" HorizontalExpand="True" PlaceHolder="{Loc 'stories-tts-search-placeholder'}" />
        </BoxContainer>

        <ScrollContainer HScrollEnabled="True" VScrollEnabled="False" MinHeight="55" Margin="0 5 0 5">
            <BoxContainer
                Name="TabsHeader"
                Orientation="Horizontal"
                SeparationOverride="4"
                HorizontalExpand="True"
                VerticalAlignment="Center"
            />
        </ScrollContainer>

        <controls:HighDivider />

        <ScrollContainer VerticalExpand="True" HorizontalExpand="True">
            <Control Name="TabsContent" HorizontalExpand="True" VerticalExpand="True" />
        </ScrollContainer>

        <BoxContainer Orientation="Horizontal" HorizontalAlignment="Center" SeparationOverride="10" Margin="0 5 0 0">
            <Button Name="RandomButton" Text="{Loc 'stories-tts-random-voice'}" />
            <Button
                Name="PreviewButton"
                Text="{Loc 'humanoid-profile-editor-voice-play'}"
                StyleClasses="ButtonColorGreen"
            />
        </BoxContainer>

    </BoxContainer>
</controls:FancyWindow>
